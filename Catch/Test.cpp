//
// Created by Chris Robinson on 3/2/23.
//
/*
#include "catch.hpp"
#include "../tsp.h"
#include <string>


TEST_CASE("Read distances.txt") {
    tsp tsp;
    REQUIRE(tsp.readDistances() == true);
}
TEST_CASE("Accept user input - getInput()") {
    tsp tsp;
    REQUIRE(tsp.acceptInput() == true);
    REQUIRE(tsp.cities == 380);
    REQUIRE(tsp.tours == 380);
    REQUIRE(tsp.generations == 380);
    REQUIRE(tsp.percentMutated == 38);
}
TEST_CASE("Store weighted edges in vector") {
    tsp.readDistances();
    REQUIRE(tsp.weightedEdges.at(0) == 25.410000);
    REQUIRE(tsp.weightedEdges.at(1) == 109.670000);
    REQUIRE(tsp.weightedEdges.at(379) == 65.180000);
}
TEST_CASE("Print s array - printS()") {

}
*/
